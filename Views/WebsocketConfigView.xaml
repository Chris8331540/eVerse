<UserControl x:Class="eVerse.Views.WebsocketConfigView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 mc:Ignorable="d">

 <Grid Margin="20">
 <StackPanel>
 <TextBlock Text="Configuracion Websocket" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

 <TextBlock Text="Direccion:" FontWeight="SemiBold" />
 <TextBlock Text="{Binding WebsocketAddress}" Margin="0,0,0,10" />

 <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
 <Button Content="Iniciar servidor" Command="{Binding StartCommand}" Margin="0,0,10,0" />
 <Button Content="Detener servidor" Command="{Binding StopCommand}" />
 </StackPanel>

 <TextBlock Text="Estado:" FontWeight="SemiBold" Margin="0,0,0,4"/>
 <TextBlock Text="{Binding IsRunningText}" />
 </StackPanel>
 </Grid>
</UserControl>